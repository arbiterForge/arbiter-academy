# Arbiter Academy Preview 0.4 promotion plan

> Historical promotion record. Preview 0.4 is an immutable release boundary. Current public
> availability, installer identity, and Pages verification are declared by the active Preview
> publication manifest and its release workflow, not by this completed plan.

**Status:** accepted under the active Arbiter Academy sprint and incremental-release authority
**Base:** `origin/main` at `b9d897c40683d7d3e4428cc3e9ec9b92368f65e0`
**Candidate branch:** `codex/academy-guided-course-design`

## Outcome

Publish the accepted guided Home, Recovery, and F01 slice as Academy Preview 0.4. Preserve all eleven runnable labs, keep F01 as the only guided lab, keep `coming_next` empty, and bind Pages to an immutable release created from the exact merged commit.

## Global constraints

- Work only in the existing isolated Academy worktree.
- Test first and observe the causal RED before production changes.
- The public release identity is `preview-0.4`; the archive is `arbiter-academy-preview-0.4.zip`.
- Preserve the exact eleven-lab runnable inventory, F01-only guided inventory, empty `coming_next`, catalog digest, and verifier authority.
- Historical test fixtures may retain old release strings when they are explicitly testing stale or generic URLs; current-release consumers may not.
- Do not publish or link an Academy console command. The website remains the course; the operations TUI remains a later integrated slice.
- Pages deployment must require a `preview-0.4` tag resolving to the exact merged main SHA and exactly six checksum-verified assets.
- Publishing requires an explicit owner confirmation to enable GitHub immutable
  releases for the Academy repository before any Preview 0.4 release is made
  public. A mutable release or draft cannot authorize Pages.
- No runtime network dependency or paid service may be added.
- Do not merge or claim deployment without exact-head CI, release, Pages, and production evidence.

### Task 1: Promote current-release identity test-first

1. Change the smallest causal release tests to require `preview-0.4`, using `preview-0.3` as the immediate stale identity where applicable.
2. Run the focused manifest, installation, site, workflow, CLI, and practitioner tests; record the expected RED caused only by current 0.3 identity.
3. Rename the publication manifest and update every current-release consumer: schema, engine, package data, action manifests, guides, README, templates, builder, checker, workflow, provenance, and current-release tests.
4. Repair the stale checker diagnostic so an empty `coming_next` failure describes the exact empty boundary rather than P06-P07.
5. Preserve valid historical and generic URL fixtures; do not add a brittle repository-wide ban on `preview-0.3`.
6. Run the focused suites and artifact build/checker to GREEN.

### Task 2: Produce deterministic immutable release assets

1. Add or finalize the reviewed PowerShell and POSIX installers plus canonical checksum files.
2. Build the Academy wheel and offline bundle twice from isolated temporary directories with deterministic environment inputs.
3. Require byte-identical wheels and `arbiter-academy-preview-0.4.zip` bundles; embed the final bundle digest in both installers.
4. Verify installer source-review, verify-first, rollback, conflict, offline wheelhouse, and manifest-owned-path contracts.
5. Stage no generated or release asset outside the exact six-file publication set.

### Task 3: Review, land, and publish Preview 0.4

1. Run focused release suites, browser tests, full governed gates, fresh artifact validation, and staged secret scanning.
2. Perform independent content/code, novice-path, visual, and exact-diff review; clear every blocking finding.
3. Stop for explicit owner confirmation that GitHub immutable releases have
   been enabled for `arbiterForge/arbiter-academy`; treat the currently disabled
   setting as an external hard gate, not an override candidate.
4. Commit and push the exact reviewed head; open an Academy PR with release hashes and evidence.
5. Wait once for exact-head hosted checks. Under the user's Academy-only merge authority, merge only the green reviewed head.
6. Build the six reviewed assets from the resulting merge SHA with the pinned
   Preview 0.4 epoch and reproduce them independently before upload.
7. Create `preview-0.4` as a draft at the exact merged main SHA, attach all six
   reviewed assets, verify their names, lengths, and hashes while still draft,
   then publish once so GitHub locks the tag and assets.
8. Let the sole main-push Pages workflow use its bounded 30-minute
   release-readiness retry (30 attempts at 60-second intervals). Deployment
   remains blocked until the release API reports
   `immutable: true`, `draft: false`, the exact main SHA and exact six uploaded
   assets; authenticated API bytes, unauthenticated public-delivery bytes, and
   assets rebuilt under pinned Python 3.12 from the exact candidate must all be
   byte-identical.
9. After Pages completes, verify Home, Recovery, F01, and `release.json` from
   production against the exact merge SHA.

## Completion evidence

- Sole current publication manifest is `preview-0.4.json` and validates.
- Focused and full exact-head test commands are green.
- Wheel and bundle reproducibility are proven by two independent builds.
- PR head, merged SHA, remote tag, GitHub Release, Pages run, and production `release.json` agree exactly.
- Repository release immutability was explicitly confirmed enabled before the
  draft was published; the published release is non-draft and reports
  `immutable: true`.
- All six authenticated downloads, unauthenticated public downloads, and exact
  candidate rebuild outputs are byte-identical.
- Production exposes eleven runnable labs, F01 guided, no coming-next links, and no nonexistent TUI command.
